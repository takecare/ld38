import Game from './Game';

window.onload = function () {
    // TODO consider wrapper for Game instead of extension
    // eslint-disable-next-line no-unused-vars
    // const game = new Game(20 * 64 / 2, 12 * 64 / 2);
    const game = new Game(800, 600);
};
